{
  "name": "chat-app-server",
  "version": "1.0.0",
  "description": "web server for chitchat",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "BikramNischal",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.9.0"
  }
}
